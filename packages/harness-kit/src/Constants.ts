/** Markdown files to skip when walking pattern directories. */
export const SKIPPED_FILES = ['CLAUDE', 'AGENTS', 'GEMINI', 'README'];

/** Default directory (relative to package root) holding migrated pattern detectors. */
export const PATTERNS_DIR = 'patterns';

/** Default directory (relative to package root) holding migrated Effect skills. */
export const SKILLS_DIR = 'skills';

/** Default directory (relative to package root) holding guidance documents. */
export const GUIDANCE_DIR = 'guidance';
