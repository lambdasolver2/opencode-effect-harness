---
"@lambda-solver/opencode-effect-harness": patch
---

Declare `output` string schemas on all tools and return the summary as structured output instead of `undefined`, so schema-strict hosts (API/Code-Mode) accept tool results. Display behavior unchanged.
